package com.nft.qa.pages;

import java.util.concurrent.TimeUnit;

import org.openqa.selenium.By;
import org.openqa.selenium.NoSuchSessionException;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.PageFactory;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

import com.b1.qa.util.TestUtility;
import com.nft.qa.base.TestBase;

public class EmailEnvelopforVendorAccount extends TestBase {

	TestUtility testUtil = new TestUtility();
	HomePage homePage = new HomePage();
	
	
	public EmailEnvelopforVendorAccount() {
		PageFactory.initElements(driver, this);
	}
	
	
	public void VendorLogin() throws InterruptedException {
		
		driver.get("https://one-staging.brandify.com/");
		Thread.sleep(5000);
		driver.findElement(By.xpath("//input[@placeholder='Username']")).sendKeys("rohit.pawar@testriq.com");
		Thread.sleep(1000);
		driver.findElement(By.xpath("//input[@placeholder='Password']")).sendKeys("Rome020295");
		Thread.sleep(5000);
		driver.findElement(By.xpath("//button[@class='raised_button signinBtn']")).click();
		Thread.sleep(5000);
	}

	public void VerifyVendorEnvelop() throws InterruptedException {
		
		Thread.sleep(5000);
		WebElement locator = driver.findElement(By.xpath("//div[contains(text(),'REVIEWS')]"));
		driver.manage().timeouts().implicitlyWait(30, TimeUnit.SECONDS);
		new WebDriverWait(driver, 60).ignoring(NoSuchSessionException.class)
				.until(ExpectedConditions.elementToBeClickable(locator));
		locator.click();
		
		Thread.sleep(3000);
		//WebElement Envolope = driver.findElement(By.xpath("/html/body/div[1]/div/div[2]/div/div[1]/ul[2]/li[2]"));
		//Assert.assertNull(Envolope);
		/*if (Envolope.isDisplayed()) {
			Assert.assertFalse(Envolope.isDisplayed());
			System.out.println("The Email Envolope is Displayed for the vendor without having Powers");
		}
		else {
			System.out.println("The Email Envolope is not Displayed for this Vendor");
		}
		*/
		if(driver.findElement(By.xpath("//ul[starts-with(@class,'list-actions sc-')]//li[starts-with(@class,'sc-')]"))!= null) {

			System.out.println("Element is Present");

			}else{

			System.out.println("Element is Absent");

			}

	
		/*	if( driver.findElement(By.cssSelector("a > font")).isDisplayed()){

			System.out.println("Element is Visible");

			}else{

			System.out.println("Element is InVisible");

			}*/
	}
}
	
